import { ReplaceLinksPipe } from './replace-links.pipe';

describe('ReplaceLinksPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceLinksPipe();
    expect(pipe).toBeTruthy();
  });
});
